# Base URL used by auth client requests.
# Default is /api (recommended for local dev with proxy).
# For deployed environments, you can set an absolute URL, for example:
# VITE_API_BASE_URL=https://api.example.com/api
VITE_API_BASE_URL=/api

# Dev proxy target for /api when running `pnpm --filter web dev`.
# If not set, defaults to http://localhost:4000.
API_PORT=4000

# Optional explicit proxy target (takes precedence over API_PORT).
# VITE_API_PROXY_TARGET=http://localhost:4000
