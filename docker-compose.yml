services:
  postgres:
    image: postgres:17-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: joygym
      POSTGRES_PASSWORD: joygym
      POSTGRES_DB: joygym
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
